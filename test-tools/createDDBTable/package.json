{
  "name": "createDDBTable",
  "version": "1.0.0",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "start": "ts-node index.ts",
    "list": "aws dynamodb list-tables --endpoint -url http://localhost:8000",
    "shell": "docker exec -t -i ddb sh"
  },
  "devDependencies": {
    "aws-sdk": "^2.458.0",
    "ts-node": "^8.1.0"
  },
  "dependencies": {
    "typescript": "^3.4.5"
  }
}
